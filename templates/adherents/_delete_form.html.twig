<form method="post" action="{{ path('app_adherents_delete', {'idAdh': adherent.idAdh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adherent.idAdh) }}">
    <button class="btn">Delete</button>
</form>
