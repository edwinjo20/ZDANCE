<form method="post" action="{{ path('app_profs_delete', {'idProf': prof.idProf}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prof.idProf) }}">
    <button class="btn">Delete</button>
</form>
